{
  "name": "seatmesh",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Typed tmux agent coordination: profiles, role index, inbox daemon, connectivity recovery.",
  "workspaces": [
    "packages/*"
  ],
  "bin": {
    "seatmesh": "./bin/seatmesh"
  },
  "scripts": {
    "build": "npm run build -w @seat-mesh/core && npm run build -w @seat-mesh/providers && npm run build -w @seat-mesh/tmux && npm run build -w @seat-mesh/connectivity && npm run build -w @seat-mesh/daemon && npm run build -w seatmesh",
    "typecheck": "npm run typecheck -ws --if-present",
    "test": "vitest run",
    "test:ci": "vitest run --exclude '**/sqlite-store.test.ts'",
    "seatmesh": "node packages/cli/dist/main.js"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0",
    "vitest": "^3.0.0"
  },
  "allowScripts": {
    "better-sqlite3@11.10.0": true
  },
  "dependencies": null
}
