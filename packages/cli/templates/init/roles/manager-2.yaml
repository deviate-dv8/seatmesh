kind: manager-2
banner:
  - "cold_start=./sm.sh whoami --validate | secondary coord — parallel framework/docs/verify slices"
read_first:
  - path: docs/SUPERVISOR-MANAGER2.md
    note: manager-2 scope + supervise room
files:
  - docs/ONE-PATH.md
  - docs/SURPASS.md
  - docs/ROLE-YAML.md
  - .sm/mesh.config.yaml
policies:
  - id: manager2_scope
    text: framework/docs/verify only — not product merge; read supervise room before claiming
  - id: next_hop_manager
    text: "NEXT HOP = manager seat, not the human. Done/blocked/need-next -> ./sm.sh room say -r managers AND ./sm.sh peer manager. Never wait for the operator."
  - id: room_supervise
    cmd: ./sm.sh room tail -r supervise -n 20
