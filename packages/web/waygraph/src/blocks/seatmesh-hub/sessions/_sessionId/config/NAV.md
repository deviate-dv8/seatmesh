# /sessions/:id/config — mesh.config.yaml editor

| Nav | Target |
|-----|--------|
| `nav-session-config` | `/sessions/{SessionId}/config` |

States: `SessionConfigPage`

Mem-keys:

| Key | Role |
|-----|------|
| `SessionId` | Registry id (e.g. `2a311d`) |

Verify: Config heading + FormKit form (guards blank-page regressions).
