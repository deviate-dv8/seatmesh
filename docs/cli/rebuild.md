# seatmesh rebuild

```text
rebuild [--layout]
  Alias of reload — rebuild engine + labels (not "reload config JSON").
```

- CLI: `seatmesh rebuild --help`
- Agent: `seatmesh agent help rebuild`
- Catalog: [../COMMANDS.md#rebuild](../COMMANDS.md#rebuild)
