# seatmesh 1.2.3

Republish after `@seat-mesh/connectivity@1.2.2` stuck in npm staging (E409), which broke `npx seatmesh@1.2.2` installs.

Same surface as 1.2.2: `npx seatmesh web up|down|restart|status`, OC-LIMIT V2 / oc-credit, `agent mds hosted|agent-self|agent <kind>`.
