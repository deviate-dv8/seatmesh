# seatmesh 1.2.5

## Fix

- Declare `yaml` as a direct dependency of the `seatmesh` CLI so global / `npx` installs resolve `merge-mesh-config` (fixes `ERR_MODULE_NOT_FOUND: Cannot find package 'yaml'`).

## Install

```bash
npm i -g seatmesh@1.2.5
# or
npx seatmesh@1.2.5 …
```
