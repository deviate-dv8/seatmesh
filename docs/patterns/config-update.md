# Config / update

**Surfaces:** CLI

1. `npm install -g seatmesh@latest` (or `npx seatmesh@latest …`)
2. `sm update` — refresh `.sm/_vendor` from **that** CLI

Guide: `sm config upgrade` · validate: `sm config check`.  
`update` alone does not bump npm.
