# One-path / discoverability

**Surfaces:** CLI

- One command per job — see `docs/ONE-PATH.md` and `sm help` (human-only) / `sm --agents help`.
- Agents: only `sm agent <cmd>` listed on **can**.
- Prefer `grep` + small docs over mega-files (this `docs/patterns/` tree).
