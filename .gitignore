# Dependencies / build
node_modules/
dist/
*.tsbuildinfo

# Tests / tooling
coverage/
.vitest/

# Local env
.env
.env.local

# Seatmesh profile / runtime (local mesh state — not product source)
.sm/
.sm-*/
**/.sm/
**/.sm-*/

# OS / editor
.DS_Store
*.log

# Local Cursor / agent workspace (not product source)
.agent/
.agents/
.cursor/
.mcp.json

# Local scripts / infra (not part of the distributed package)
scripts/
docker-compose.*.yml
